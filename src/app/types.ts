export interface Listing {
    id: string,
    name: string,
    description: string,
    price: number,
    views: number,
};

export interface ListingStub {
    name: string,
    description: string,
    price: number,
};